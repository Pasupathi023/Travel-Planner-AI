version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build..."
    build:
      commands:
        - docker build -t travel-planner .
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
